<h1>{{title}}</h1>

<ul>
    {{#each carts}}
    <li> CartID: ({{this._id}})
        <p>Products:</p>
        <ul>
            {{#each this.products}}
            <li>{{this.title}} - {{this.price}}</li>
            {{/each}}
        </ul>
    </li>
    {{/each}}
</ul> 